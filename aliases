## Main application.
alias start="docker-compose build && docker-compose up"
alias stop="docker-compose down --rmi local"
alias build="rm -Rf docs && docker-compose run --rm -e JEKYLL_ENV=production web jekyll build"
